// TODO: delete if not needed!

// const Client = require('@veryfi/veryfi-sdk');
const client_id = process.env.NEXT_PUBLIC_VERYFI_CLIENT_ID
const client_secret = process.env.NEXT_PUBLIC_VERYFI_CLIENT_SECRET
const username = process.env.NEXT_PUBLIC_VERYFI_USERNAME
const api_key = process.env.NEXT_PUBLIC_VERYFI_API_KEY

let categories = ['Grocery', 'Utilities', 'Travel']

// currently gets one
// let veryfi_client = new Client(client_id, client_secret, username, api_key)


export const getVeryfiData = async (file_paths: any) => {
  // let dataOut: any = []

  // await Promise.all(
  //   file_paths.map(async (file_path: any) => {
  //     let response = await veryfi_client
  //       .process_document(file_path, (categories = categories))
  //       .promise()
  //     console.log('response from Veryfi', response)
  //     dataOut.push(response)
  //   }),
  // )
  // return dataOut
}



// old references: https://github.com/veryfi/veryfi-nodejs
